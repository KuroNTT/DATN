// const express = require("express");
// const router = express.Router();
// const PriceRangeModel = require("../models/PriceRanges");


// /* Lấy tất cả danh mục */
// router.get("/", async (req, res) => {
//   const price_ranges = await PriceRangeModel.findAll({
//   });
//   res.json(price_ranges);
// });

// /* Lấy danh mục theo id*/
// router.get("/:id", async (req, res) => {
//   const price_range = await PriceRangeModel.findByPk(req.params.id);
//   res.json(price_range);
// });

// module.exports = router;
